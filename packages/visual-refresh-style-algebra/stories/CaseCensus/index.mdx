import { Meta, Stories, Title } from "@storybook/addon-docs/blocks";
import * as CaseCensusStories from "./index.stories";

<Meta of={CaseCensusStories} />

<Title />

This page continues the introductory path from [Start here](?path=/docs/packages-visual-refresh-style-algebra-start-here--docs). It explains where the model's two census numbers come from before later pages begin sampling individual buttons.

## The simple idea

A button case is a tuple of eleven choices: product, visual language, density, appearance, interaction state, color mode, content kind, icon placement, anatomy policy, composition, and direction.

If every value is paired with every other value, the result is the **raw Cartesian space**. The **model-admitted space** keeps only tuples accepted by the clean-room predicate.

The distinction is about this clean-room model's contract. An excluded tuple is outside the modeled domain; it is not a claim that Fluent or CAP rejects it, or that no product could support it. The resulting 27,840 is a model-derived count, not a production CAP inventory.

## Follow the four steps

1. Multiply the cardinalities of all eleven axes to get 138,240 raw tuples.
2. See the clean-room anatomy hypothesis that admits four of twelve abstract content and icon-placement pairs.
3. Count the assumed appearance and anatomy domains within each product and visual language, producing 29 rather than 48.
4. Recombine the independent groups: `240 × 4 × 29 = 27,840` model-admitted cases.

<Stories includePrimary title={null} />

## Why this matters for testing

Only about 20.1% of the raw tuples satisfy the model predicate. A generator that chose all eleven axes independently would therefore spend roughly four fifths of its samples producing inputs the clean-room resolver rejects. The package instead generates within those declared relationships, so property runs spend their effort challenging the resolver's promised domain.

The visualization and the exhaustive metric use the same exported domain declarations. `researchMetrics.test.ts` still enumerates every one of the 138,240 tuples and checks that exactly 27,840 satisfy the model predicate.

Continue to [Content anatomy grounding](?path=/docs/packages-visual-refresh-style-algebra-content-anatomy-grounding--docs) to see the twelve production-shaped scenarios exercised through real Fluent normalization, rendering, and VR CAP styling. Then use [Button comparison](?path=/docs/packages-visual-refresh-style-algebra-button-comparison--docs) to see how a few carefully held conditions make product and visual-language differences reviewable.
