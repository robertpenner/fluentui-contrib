import { Canvas, Meta, Title } from "@storybook/addon-docs/blocks";
import * as CapAuditIntroductionStories from "./index.stories";

<Meta of={CapAuditIntroductionStories} />

<Title>CAP Button audit</Title>

The production CAP Button hook does not define the earlier eleven-dimensional, 138,240-case space. This audit now starts from the fields and slot-presence checks the real Fluent normalization and CAP style code use: 2,592 scoped authored scenarios normalize to 1,728 state tuples and 504 distinct style-selection profiles.

<Canvas of={CapAuditIntroductionStories.CapAuditIntroduction} />

Open the [Content anatomy audit](?path=/docs/packages-visual-refresh-style-algebra-content-anatomy-audit--docs) to inspect all twelve results.
