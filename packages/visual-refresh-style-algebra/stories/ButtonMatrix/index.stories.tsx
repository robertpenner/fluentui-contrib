import type { Meta } from '@storybook/react';
import { ButtonMatrix } from './ButtonMatrix.stories';

const meta = {
  title: 'Packages/visual-refresh-style-algebra/Curated matrix',
  component: ButtonMatrix,
} satisfies Meta<typeof ButtonMatrix>;

export default meta;
export { ButtonMatrix } from './ButtonMatrix.stories';
