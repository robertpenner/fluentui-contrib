import type { Meta } from '@storybook/react';
import { ToggleButtonSpecializationLaws } from './ToggleButtonLaws.stories';

const meta = {
  title: 'Packages/visual-refresh-style-algebra/CAP ToggleButton laws',
  component: ToggleButtonSpecializationLaws,
} satisfies Meta<typeof ToggleButtonSpecializationLaws>;

export default meta;
export { ToggleButtonSpecializationLaws } from './ToggleButtonLaws.stories';
