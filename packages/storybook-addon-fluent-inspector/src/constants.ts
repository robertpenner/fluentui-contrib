export const ADDON_ID = 'fluentui-contrib/fluent-inspector';
export const PANEL_ID = `${ADDON_ID}/panel`;

export const EVENTS = {
  TREE: `${ADDON_ID}/tree`,
  SELECT: `${ADDON_ID}/select`,
  REFRESH: `${ADDON_ID}/refresh`,
} as const;
